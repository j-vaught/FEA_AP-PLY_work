# Stage 02 Figure Gallery

`stage_02_mesh_wireframe.png` shows the reconstructed undeformed coarse HEXA cantilever mesh from `stage02_explicit_coarse_a1p00A001.vtk` at solver time 1.0 s, with axes in metres; `stage_02_deformed.png` shows the same snapshot warped once by the recorded `Displacement` vector field and coloured by displacement magnitude in millimetres; `stage_02_no_animation.txt` records that only A001 was available, so no MP4 was generated; and `stage_02_timeseries.typ` / `stage_02_timeseries.pdf` plot the OpenRadioss tip sag against the Bisshopp-Drucker elastica reference from `results/timeseries.csv`, including the 2% gate. The `results.json` verdict is INCONCLUSIVE because this output used the explicit dynamic-relaxation fallback rather than the gated baseline implicit solve; the available coarse alpha=1.0 row is marked FAIL with 99.99% `dy/L` error.
