# Stage 01 Figure Gallery

`stage_01_mesh_wireframe.png` shows the reconstructed undeformed M1 four-point-bending HEXA mesh from `stage01_4pt_M1A001.vtk` at solver time 0.00600001 s, with axes in metres; `stage_01_deformed.png` shows the same snapshot warped once by the recorded `Displacement` vector field and coloured by displacement magnitude in millimetres; `stage_01_animation.mp4` loops the two available three-point M0 animation frames from `stage01_3pt_M0A001.gz` and `stage01_3pt_M0A002.gz` for at least 4 s at 24 fps; and `stage_01_timeseries.typ` / `stage_01_timeseries.pdf` plot Euler-Bernoulli relative error from `results/timeseries.csv` against the 1% pass gate. The `results.json` verdict is FAIL: the gated `4pt_M1` displacement error is 49.516% relative to the Euler-Bernoulli reference.
